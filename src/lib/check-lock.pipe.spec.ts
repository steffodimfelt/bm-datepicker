import { CheckLockPipe } from './check-lock.pipe';

describe('CheckLockPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CheckLockPipe();
    expect(pipe).toBeTruthy();
  });
});
